			1.3 version
			-------------
incompatibility : none
bugfix : should now work with c-shell
new features : none
----------------------------------------------------------------
			1.2 version
			-------------
incompatibility : none
bugfix : fix localisation problem
new features : 
- rc file is now loaded in order : host, home, local
- better parsing for "rpm -V" output
----------------------------------------------------------------
			1.1 version
			-------------
incompatibility : none
bugfix : fix a bug for directories
new features : none
----------------------------------------------------------------
			1.0 version
			-------------
incompatibility :
bugfix :
new features :
- french translation in spec file for rpm
----------------------------------------------------------------
			0.9 version
			-------------

incompatibility : none
bugfix : none
new features :
* add more tests (if package exists, if file exists ...)
* add more infos
* add regression tests (test_rpmrestore.pl)
----------------------------------------------------------------
			0.8 version
			-------------
incompatibility : 
* the log format was changed (previous log will not work with this release)
* default behavior is to work on all attributes

bugfix : 
* rollback was fixed

new features :
* can use rcfile
* recode rollback to use options (batch, dryrun, attributes choice)
----------------------------------------------------------------
			0.3 version
			-------------
add md5 attribute (compare only)
----------------------------------------------------------------
			0.2 version
			-------------

test for superuser account and force dry-run if not
----------------------------------------------------------------
			0.1 version
			-------------

first public release
