send me your features request ...

text translation (internationalisation)

change rpm category ?

graphical interface

1.4
posix capabilities
	- check if capabilities exists : seach for rpm --querytags | grep CAPABILITY + getcap/setcap
	- detect capa changes (rpm -V => P
